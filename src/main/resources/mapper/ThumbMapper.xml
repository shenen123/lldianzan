<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liubinrui.mapper.ThumbMapper">

    <resultMap id="BaseResultMap" type="com.liubinrui.model.entity.Thumb">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="userId" column="userId" jdbcType="BIGINT"/>
        <result property="blogId" column="blogId" jdbcType="BIGINT"/>
        <result property="createTime" column="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,userId,blogId,createTime
    </sql>

    <insert id="batchInsertIgnore">
        INSERT IGNORE INTO thumb (userId, blogId, createTime)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.userId}, #{item.blogId}, #{item.createTime})
        </foreach>
    </insert>

    <delete id="batchDeleteByUserAnalog">
        DELETE FROM thumb
        WHERE (userId, blogId) IN
        <foreach collection="list" item="param" open="(" close=")" separator=",">
            (#{param.userId}, #{param.blogId})
        </foreach>
    </delete>

</mapper>
