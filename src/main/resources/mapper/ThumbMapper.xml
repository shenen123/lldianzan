<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liubinrui.mapper.ThumbMapper">

    <resultMap id="BaseResultMap" type="com.liubinrui.model.entity.Thumb">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="blogId" column="blog_id" jdbcType="BIGINT"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, blog_id, create_time
    </sql>

    <insert id="batchInsertIgnore">
        INSERT IGNORE INTO thumb (user_id, blog_id, create_time)
        VALUES
        <foreach collection="thumbList" item="item" separator=",">
            (#{item.userId}, #{item.blogId}, #{item.createTime})
        </foreach>
    </insert>

    <delete id="batchDelete">
        DELETE FROM thumb
        WHERE
        <foreach collection="deleteList" item="item" separator="OR">
            (user_id = #{item.userId} AND blog_id = #{item.blogId})
        </foreach>
    </delete>

</mapper>
